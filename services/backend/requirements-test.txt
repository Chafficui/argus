# requirements-test.txt
# Install with: pip install -r requirements-test.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2           # Async HTTP client — for calling the FastAPI test server

# testcontainers spins up real Docker containers for integration tests.
# No mocking — actual Postgres, actual Milvus. Tests are slower but much more realistic.
testcontainers[postgres]==4.8.1

# For mocking external services in unit tests
pytest-mock==3.14.0

# For generating realistic fake data in tests
faker==30.3.0
